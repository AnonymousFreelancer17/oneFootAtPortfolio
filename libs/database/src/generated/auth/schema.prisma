generator client {
  provider = "prisma-client-js"
  output   = "../src/generated/auth"
}

datasource db {
  provider = "mongodb"
}

model User {
  id    String  @id @default(auto()) @map("_id") @db.ObjectId
  email String  @unique
  name  String?
}
